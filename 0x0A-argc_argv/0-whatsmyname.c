#include <stdio.h>
#include <stdlib.h>
/**
 * main - function that print the name of the files
 * @argc: count Argument parameters
 * @argv: Array Argument parametes
 * Return: Always 0 (Success)
 */
int main(int argc, char *argv[])
{
	  printf("%s\n", argv[0]);
}
