#include "main.h"
/**
 * _abs - print the absolute value of numbers
 *
 * @r: number that was changed with abs
 * Return: Always 0 (Success)
 */
int _abs(int r)
{
if (r >= 0)
{
return (i);
}
else
{
return (i * -1);
}
