#include<stdlib.h>
#include<stdio.h>
/**
 * main - determine the 10 digits of the number
 *
 * Return: Always 0 (success)
 */
int main(void)
{
for (int i = 0; i <= 10; i++)
{
printf("%d", i);
}
printf("\n");
return (0);
}
